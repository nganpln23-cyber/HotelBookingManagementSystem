<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- Đọc biến môi trường (DB_URL, DB_USERNAME, DB_PASSWORD, ...) -->
    <context:property-placeholder system-properties-mode="OVERRIDE" />

    <!-- Quét các annotation @Controller, @Service, @Repository -->
    <context:component-scan base-package="com.hotel.controller" />
    <context:component-scan base-package="com.hotel.service" />
    <context:component-scan base-package="com.hotel.repository" />
    <context:component-scan base-package="com.hotel.web" />

    <!-- Bật @GetMapping, @PostMapping, @RequestMapping, @ModelAttribute... -->
    <mvc:annotation-driven />

    <!-- Cho phép upload file (multipart/form-data) -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.support.StandardServletMultipartResolver" />

    <!-- Chặn truy cập /admin/** nếu chưa đăng nhập -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/admin/**" />
            <ref bean="loginInterceptor" />
        </mvc:interceptor>
        <mvc:interceptor>
            <mvc:mapping path="/admin/**" />
            <ref bean="roleInterceptor" />
        </mvc:interceptor>
        <mvc:interceptor>
            <mvc:mapping path="/account/**" />
            <mvc:exclude-mapping path="/account/login" />
            <mvc:exclude-mapping path="/account/register" />
            <ref bean="customerLoginInterceptor" />
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- Cho phép load CSS/JS/Image trong thư mục /assets/ -->
    <mvc:resources mapping="/assets/**" location="/assets/" />
    <mvc:default-servlet-handler />

    <!-- View Resolver: controller trả về "rooms/list" thì mở /WEB-INF/views/rooms/list.jsp -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <!--
        Kết nối database.
        Đặt biến môi trường trên Render:
          DB_URL      = jdbc:mysql://<host>:<port>/<dbname>?useSSL=true&allowPublicKeyRetrieval=true&serverTimezone=Asia/Ho_Chi_Minh&characterEncoding=UTF-8
          DB_USERNAME = <username>
          DB_PASSWORD = <password>
        Mặc định (local dev): localhost:3307, root, không mật khẩu.
    -->
    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="com.mysql.cj.jdbc.Driver" />
        <property name="url" value="#{systemEnvironment['DB_URL'] ?: 'jdbc:mysql://localhost:3307/hotel_booking_db?useSSL=false&amp;allowPublicKeyRetrieval=true&amp;serverTimezone=Asia/Ho_Chi_Minh&amp;characterEncoding=UTF-8'}" />
        <property name="username" value="#{systemEnvironment['DB_USERNAME'] ?: 'root'}" />
        <property name="password" value="#{systemEnvironment['DB_PASSWORD'] ?: ''}" />
    </bean>

    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <constructor-arg ref="dataSource" />
    </bean>

    <!-- Email (Gmail SMTP). Đặt biến môi trường MAIL_USERNAME và MAIL_PASSWORD trên Render. -->
    <bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
        <property name="host" value="smtp.gmail.com" />
        <property name="port" value="587" />
        <property name="username" value="#{systemEnvironment['MAIL_USERNAME'] ?: 'YOUR_EMAIL@gmail.com'}" />
        <property name="password" value="#{systemEnvironment['MAIL_PASSWORD'] ?: 'YOUR_APP_PASSWORD'}" />
        <property name="javaMailProperties">
            <props>
                <prop key="mail.smtp.auth">true</prop>
                <prop key="mail.smtp.starttls.enable">true</prop>
                <prop key="mail.smtp.connectiontimeout">5000</prop>
                <prop key="mail.smtp.timeout">5000</prop>
                <prop key="mail.smtp.writetimeout">5000</prop>
            </props>
        </property>
    </bean>

</beans>
